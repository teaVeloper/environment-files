	colorscheme koehler     "specific colorscheme
	syntax enable           " enables syntax highlighting
	set tabstop=2           "defines tab as 2 spaces
	set softtabstop=2       "makes tab to be length of 2 spaces when used
	set expandtab           "makes tab also be rendered as if pressed 2 times space
	set number              "enable line numbers
	set cursorline          "enable a vertical line where the cursor is
	set wildmenu            "enable wildcart menu
	set showmatch           "showing matching possibilities
